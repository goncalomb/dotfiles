#!/usr/bin/env bash

# /// dotfiles
# description = "Lists the directories in PATH."
# author = "goncalomb"
# year = "2026"
# tags = ["stable"]
# ///

set -euo pipefail

echo "PATH=$PATH"
echo
echo "$PATH" | tr ':' '\n'
echo
echo "$PATH" | tr ':' '\n' | sort | uniq -c | awk '$1 > 1' | sort -n
